# ConcertMVP

Две независимые реализации сайта Концертного центра «Сириус»:

- [Astro](https://miggrabbid.github.io/ConcertMVP/astro/)
- [Gatsby + React](https://miggrabbid.github.io/ConcertMVP/getsby/)

Обе версии полностью формируют контент и SEO-разметку во время сборки (SSG). Runtime SSR не используется: сайт публикуется на GitHub Pages, где нет серверного процесса для рендеринга по запросу, а текущие данные не требуют обновления между сборками.

В Astro нет клиентского framework-runtime: меню, карусель, фильтр афиши и reveal-анимации работают на небольших DOM-контроллерах. Gatsby также генерирует готовый HTML, после чего штатно гидратирует React-компоненты для интерактивности.

## Структура репозитория

| Путь | Назначение |
| --- | --- |
| `astro/` | Самостоятельная версия на Astro 5 |
| `astro/src/pages/` | Тонкий файловый routing-слой для русских и английских страниц |
| `astro/src/templates/` | Шаблоны главной страницы и страницы концерта |
| `astro/src/layouts/` | HTML/SEO-layout и общий site layout |
| `astro/src/components/` | Статические layout-, section- и UI-компоненты Astro |
| `astro/src/app/data/` | Типизированные build-time данные сайта и концертов |
| `astro/src/app/` | Данные, переводы, типы, утилиты и общие стили |
| `astro/public/` | Статические изображения и шрифты Astro-версии |
| `getsby/` | Самостоятельная версия на Gatsby 5 и React 18. Имя каталога также используется в публичном URL |
| `getsby/src/templates/` | React-шаблоны главной страницы и страницы концерта |
| `getsby/src/components/layout/` | Общий site layout, шапка, подвал и социальные ссылки |
| `getsby/src/components/sections/` | Секции главной страницы и страницы концерта |
| `getsby/src/components/seo/` | Общий компонент SEO-метаданных |
| `getsby/src/data/` | Типизированные build-time данные сайта и концертов |
| `getsby/src/` | Компоненты, данные, переводы, типы, утилиты и стили, разделённые по верхнеуровневым каталогам |
| `getsby/static/` | Статические изображения и шрифты Gatsby-версии |
| `getsby/gatsby-node.ts` | Создание всех локализованных страниц и передача данных в шаблоны |
| `pages-root/` | Файлы корня итогового сайта GitHub Pages |
| `.github/workflows/deploy-pages.yml` | Проверка, сборка и единая публикация обеих версий |
| `reference-assets/` | Референсы и вспомогательные материалы для визуальной сверки |

Общие прикладные слои обеих реализаций:

| Каталог | Ответственность |
| --- | --- |
| `data/` | Структурированные данные, читаемые только во время сборки |
| `i18n/` | Типизированные русские и английские подписи интерфейса |
| `lib/` | Чистые функции для маршрутов, дат и служебных преобразований |
| `types/` | TypeScript-модели данных |
| `styles/` | Общие стили и подключение шрифтов |

## Маршруты

Сейчас в каждой версии реализованы две страницы: главная и страница концерта. Страница концерта строится из одного шаблона для разных наборов данных. Для каждой страницы также создаётся отдельная английская версия.

| Страница | Маршрут внутри версии |
| --- | --- |
| Главная | `/` |
| Главная, английская версия | `/en/` |
| Концерт | `/projects/{projectId}/concerts/{concertId}/` |
| Концерт, английская версия | `/en/projects/{projectId}/concerts/{concertId}/` |

При публикации маршруты получают префикс соответствующей версии: `/ConcertMVP/astro/` или `/ConcertMVP/getsby/`.

## SSG и CSR

### Astro

| Область | Режим | Реализация |
| --- | --- | --- |
| Маршруты и данные | SSG | Файлы в `src/pages/` выбирают локаль и build-time данные; `getStaticPaths()` создаёт страницы концертов |
| Шаблоны | SSG | `HomePage.astro` и `ConcertPage.astro` собирают страницы из layout- и section-компонентов |
| Layout и SEO | SSG | `BaseLayout.astro` создаёт HTML-документ, canonical, `hreflang`, Open Graph и JSON-LD; `SiteLayout.astro` добавляет общую оболочку |
| Локализация | SSG | Русский и английский контент публикуются на отдельных URL с корректным `lang` и взаимными `hreflang` |
| Интерактивность | CSR без hydration | Небольшие нативные скрипты управляют меню, каруселью, фильтром и `IntersectionObserver`; контент на клиенте не загружается повторно |

Astro-версия не содержит React и не отправляет framework-runtime в браузер.

### Gatsby

| Область | Режим | Реализация |
| --- | --- | --- |
| Маршруты и данные | SSG | `gatsby-node.ts` создаёт все RU/EN страницы и передаёт сериализуемые build-time данные через `pageContext` |
| Шаблоны | SSG | `src/templates/home.tsx` и `src/templates/concert.tsx` формируют страницы через общий `SiteLayout` и переиспользуемые секции |
| SEO | SSG | Экспорты `Head` используют общий `Seo` для canonical, `hreflang`, Open Graph и JSON-LD |
| React-интерфейс | SSG + hydration | Gatsby создаёт полный HTML во время build, затем штатно гидратирует React-дерево страницы |
| Интерактивность | CSR | После гидратации работают меню, карусель, фильтры и reveal-анимации; клиентских запросов за основным контентом нет |

### Граница между SSG и CSR

| Сценарий | Где выполняется |
| --- | --- |
| Построение маршрутов и HTML всех страниц | На этапе сборки (SSG) |
| Русский и английский контент | Встраивается в отдельные HTML-страницы при SSG |
| SEO-метаданные и структурированные данные концертов | Создаются при SSG |
| Переключение языка | Переход между заранее собранными локализованными URL |
| Интерактивность Astro | Нативные DOM-контроллеры в браузере, без hydration |
| Интерактивность Gatsby | React hydration в браузере поверх готового HTML |
| Загрузка основного контента после открытия страницы | Не требуется |

## Сводное сравнение Astro и Gatsby

Это общее сравнение фреймворков применительно к целевому сайту и основано на характере используемых данных, интерфейса и стилей:

- всего планируется 4–5 страниц: 2 уже реализованы, ещё 2–3 могут быть добавлены;
- основа сайта — структурированный двуязычный контент: программы, события, описательные разделы, новости и контакты;
- в дизайне важны крупные изображения, выразительная типографика, адаптивные сетки и CSS-анимации;
- клиентская интерактивность ограничена отдельными элементами: меню, фильтрами, каруселями и переключением языка;
- для контентного сайта важны SEO, адаптивность и быстрая первоначальная загрузка;
- не предполагаются сложный личный кабинет, совместное редактирование, real-time-функции или большое глобальное клиентское состояние.

Сравнение не опирается на внутреннюю архитектуру текущих прототипов Astro и Gatsby.

| Критерий | Astro: плюсы | Astro: минусы | Gatsby: плюсы | Gatsby: минусы |
| --- | --- | --- | --- | --- |
| Основная модель | Контент сначала превращается в HTML, а JavaScript подключается только там, где нужна интерактивность. Это естественная модель для сайта данного масштаба. | Подход отличается от привычной архитектуры единого React-приложения и требует понимания границы между статическим HTML и интерактивными islands. | Весь сайт строится как единое React-приложение — модель знакома React-разработчикам и удобна при большом количестве интерактивных компонентов. | Для преимущественно контентного сайта цельное React-дерево и клиентский runtime могут быть избыточны. |
| Производительность в браузере | Astro-компоненты не добавляют клиентский JavaScript по умолчанию; можно независимо выбирать момент загрузки каждого интерактивного блока. | Если обернуть страницу в один большой React-компонент, преимущество частичной гидратации будет почти потеряно. | Gatsby выполняет code splitting, предварительно создаёт HTML и может заранее загружать ресурсы внутренних страниц. | React-гидратация и Gatsby runtime увеличивают базовую стоимость JavaScript даже для относительно статичных страниц. |
| Масштаб в 4–5 страниц | Такой небольшой сайт без проблем покрывается обычным SSG; добавление ещё 2–3 страниц практически не меняет архитектуру и не создаёт заметной сборочной нагрузки. | Для каждой новой интерактивной области нужно осознанно выбирать способ гидратации и не допускать ненужного дробления интерфейса. | SSG также легко обслужит этот объём; генерация URL из данных и шаблонов является стандартным сценарием Gatsby. | Преимущества сложного Data Layer на таком объёме могут не компенсировать дополнительную конфигурацию и количество абстракций. |
| Маршрутизация | Простая файловая маршрутизация; статические и динамические маршруты располагаются непосредственно в `src/pages`. | Для поведения сложного SPA может потребоваться клиентский роутер или более крупный framework-компонент. | Файловые страницы сочетаются с программным созданием маршрутов; `<Link>` поддерживает быструю клиентскую навигацию и prefetch. | Программная генерация страниц, Node API и шаблоны сложнее обычной файловой маршрутизации. |
| Интерактивность | React, Vue, Svelte, Solid и другие UI-фреймворки можно подключать точечно и гидратировать через `client:load`, `client:idle` или `client:visible`. | Общее состояние и тесное взаимодействие между независимыми islands организовать сложнее, чем внутри одного React-дерева. | React-состояние, Context, клиентская маршрутизация и сложные связанные сценарии естественно работают в едином дереве. | Даже статичные области обычно становятся частью гидратируемого React-приложения. |
| Контент и данные | Есть типизированные content collections; данные можно получать напрямую из файлов, API или CMS без обязательного GraphQL-слоя. | Для разных CMS может потребоваться самостоятельно выбрать SDK, схему данных и стратегию кеширования либо искать отдельную интеграцию. | GraphQL Data Layer объединяет данные из CMS, файлов, API и баз данных в общую схему; source-плагины скрывают значительную часть интеграционной работы. | GraphQL, схема Gatsby и lifecycle API повышают порог входа и могут быть неоправданными при одном простом источнике данных. |
| SEO | Статический HTML создаётся по умолчанию; layouts позволяют централизовать meta-теги и структурированные данные. | Готового универсального SEO-слоя нет — его нужно спроектировать самостоятельно или подключить интеграцию. | SSG формирует индексируемый HTML; `Head` API и плагины позволяют организовать SEO, sitemap и RSS. | SEO часто зависит от качества выбранных плагинов и принятых в проекте соглашений. |
| Изображения | `astro:assets` поддерживает оптимизацию, responsive-изображения и генерацию подходящих форматов без обязательного GraphQL. | Возможности обработки зависят от типа источника и выбранного deployment-адаптера; для удалённых изображений нужна корректная конфигурация. | `gatsby-plugin-image` предоставляет зрелый конвейер responsive-изображений, placeholder-эффекты и интеграцию с CMS через Data Layer. | Обработка изображений добавляет зависимости и способна заметно увеличить время и потребление памяти при сборке. |
| Режимы рендеринга | По умолчанию используется SSG; отдельные маршруты можно перевести на on-demand SSR, сохранив остальные статическими. | Для SSR необходим адаптер конкретной платформы, а доступные возможности могут различаться между адаптерами. | Можно выбирать SSG, SSR или DSG на уровне страниц; DSG полезен для очень крупных наборов редко посещаемых страниц. | SSR и DSG требуют совместимой серверной платформы; для сайта из 4–5 страниц DSG практической пользы почти не даёт. |
| Сборка и CI | Для небольшого контентного сайта сборка обычно остаётся простой: Astro генерирует HTML и собирает только явно подключённый клиентский код. | Производительность зависит от дисциплины: крупные islands и тяжёлые UI-зависимости способны увеличить бандл независимо от фреймворка. | Gatsby имеет развитый build pipeline, кеширование, обработку данных, страниц и изображений. | С ростом контента, GraphQL-запросов, плагинов и обработки изображений сборка становится тяжелее; Gatsby отдельно документирует необходимость её оптимизации. |
| Экосистема | Можно использовать npm-пакеты и компоненты нескольких UI-фреймворков; архитектура не привязывает проект только к React. | Экосистема готовых CMS-интеграций и высокоуровневых плагинов менее централизована, поэтому иногда требуется ручная интеграция. | Большая Gatsby-экосистема включает source-плагины, CMS-интеграции, изображения, аналитику, sitemap и другие готовые решения. | Качество и актуальность community-плагинов различаются; обновление Gatsby может потребовать синхронного обновления нескольких интеграций. |
| Команда и поддержка | Удобен, если команда готова разделять статические и интерактивные части и хочет контролировать количество JavaScript. | Нужно освоить синтаксис `.astro`, директивы гидратации и правила взаимодействия Astro с React или другим UI-фреймворком. | Удобен для React-команды: страницы и компоненты остаются в привычной JSX/TSX-модели. | Помимо React необходимо понимать GraphQL, Gatsby Node API, lifecycle API, плагины и особенности сборки. |
| Возможное усложнение продукта | Можно постепенно добавлять islands или переводить отдельные маршруты на SSR, не меняя режим всего сайта. | Если продукт превратится в насыщенное SPA с большим общим состоянием, Astro станет оболочкой вокруг крупного React-приложения и даст меньше преимуществ. | Лучше подготовлен к интерфейсу, где почти каждая страница тесно связана с React-состоянием и клиентской навигацией. | Если развитие останется в пределах нескольких контентных страниц, дополнительная инфраструктура продолжит давать мало практической отдачи. |

### Вывод

Для сайта из 4–5 преимущественно контентных страниц с текущей структурой данных и визуально насыщенными адаптивными стилями предпочтительнее **Astro**. Он позволяет сформировать SEO-дружественный HTML на этапе сборки и подключать JavaScript только для меню, фильтров, каруселей и других интерактивных элементов. Стили, анимации, изображения и двуязычный контент при этом не требуют единого клиентского React-приложения.

**Gatsby** также решает поставленную задачу, но его Data Layer, GraphQL и дополнительная инфраструктура не дают существенного преимущества при небольшом количестве страниц и простых источниках данных. Gatsby станет более обоснованным выбором, если появятся несколько связанных источников данных, обязательные Gatsby-интеграции или сложные сценарии с общим клиентским состоянием.

Официальная документация: [Astro Components](https://docs.astro.build/en/basics/astro-components/), [Astro Rendering](https://docs.astro.build/en/guides/on-demand-rendering/), [Gatsby Data Layer](https://www.gatsbyjs.com/docs/reference/graphql-data-layer/graphql-api/), [Gatsby Rendering](https://www.gatsbyjs.com/docs/reference/rendering-options/), [Gatsby Link](https://www.gatsbyjs.com/docs/reference/built-in-components/gatsby-link/).
